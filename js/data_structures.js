// release 0
var horseNames = ["dom", "Kayden", "Amy", "Mike"];
var colors = ["red", "blue", "yellow", "green"];

colors.push("white");
console.log(colors);

horseNames.push("Scooby");
console.log(horseNames);

// release 1

// var finalArray = {};
// for (var i = 0; i < horseNames.length; i++) {
//   finalArray.push(horseNames[i]);
// }
// return finalArray;

// horse['name'] = horseNames[i];
// horse['color'] = colors[i];





// var finalArray = {};
// for (var i = 0; i < horseNames.length; i++) {
//   finalArray.push(horseNames[i]);
// }
// return finalArray;

finalArray.push = (horseNames[i], colors[i]);
